import { PatientPipePipe } from './patient-pipe.pipe';

describe('PatientPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PatientPipePipe();
    expect(pipe).toBeTruthy();
  });
});
